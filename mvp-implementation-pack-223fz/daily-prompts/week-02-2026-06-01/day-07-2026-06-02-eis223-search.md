# Day 07: 2026-06-02 - FastAPI EIS223 search endpoint

## Goal

Реализовать `POST /v1/eis223/search`, который принимает параметры `SavedFilter`, ходит в provider abstraction и возвращает normalized hits для Next.js без записи в БД.

## Implementation prompt

```text
Ты senior Python/FastAPI engineer. Сегодня Day 07 Sprint 02 MVP Operational Workspace по 223-ФЗ.

Режим экономии контекста:
- Этот prompt самодостаточен. Не перечитывай deep-research-report.md и mvp-implementation-pack-223fz через shell.
- Читай только релевантный код apps/api provider/router/schemas/tests.
- Перед запуском любого сервера проверь `.agent-state/runtime-status.json`; если нужный сервис уже `running` и URL отвечает, используй его, а не запускай новый порт.
- После запуска/остановки/проверки долгоживущего процесса обнови `.agent-state/runtime-status.json`.
- Открывай статичные markdown-документы только если prompt неполный или противоречивый.

Исходное состояние:
- Day 06 должен был создать EIS223Provider, fixture/live mode, settings, schemas и typed errors.
- FastAPI не пишет в PostgreSQL.
- Next.js позже будет валидировать DTO и писать через Prisma.

Задача дня: endpoint поиска.

Сделай только это:
1. Реализуй `POST /v1/eis223/search`.
2. Endpoint принимает `SavedFilterExecutionRequest`:
   - filterId
   - searchQuery
   - includeKeywords
   - excludeKeywords
   - okpd2Prefixes
   - regionCodes
   - methodAllowList
   - customerInnAllowList
   - customerInnBlockList
   - minPrice
   - maxPrice
   - daysAhead
   - cursor
3. Возвращает response:
   - hits: NormalizedTenderHit[]
   - nextCursor: string|null
   - providerMode: fixture|live
   - sourceFreshness: строка/объект без обещания real-time
4. Реализуй фильтрацию в fixture provider:
   - include/exclude keywords
   - regionCodes
   - methodAllowList
   - customer allow/block INN
   - min/max price
   - daysAhead по applicationDeadlineAt
5. Для live provider добавь mapping request -> upstream query настолько, насколько позволяет текущий внешний API. Если API key недоступен, не имитируй live success: верни typed provider_not_configured.
6. Добавь tests:
   - happy path fixture
   - empty result
   - include/exclude keywords
   - price range
   - daysAhead
   - provider_not_configured

Жесткие ограничения:
- Не делай normalize endpoint сегодня.
- Не делай Next cron/upsert.
- Не пиши в PostgreSQL.
- Не делай AI/alerts/scoring.
- Не называй fixture данные реальными.

Acceptance criteria:
- POST /v1/eis223/search работает в fixture mode.
- Response стабилен и пригоден для contract validation на стороне Next.js.
- Пустой результат возвращает hits: [].
- Upstream/config errors возвращаются typed.
- Tests покрывают фильтры и ошибки.

После реализации верни:
- endpoint contract;
- пример request/response;
- tests run;
- known limitations live provider.
```

## QA prompt

```text
Ты QA/reviewer агент. Проверь результат Day 07.

Режим экономии контекста:
- Этот QA prompt самодостаточен. Не перечитывай статичные markdown-документы через shell.
- Проверяй только FastAPI router/provider/schemas/tests.
- Перед запуском/проверкой сервера посмотри `.agent-state/runtime-status.json` и используй уже запущенный URL, если он отвечает.

Проверь:
1. POST /v1/eis223/search принимает нужные поля.
2. Response содержит hits, nextCursor, providerMode, sourceFreshness.
3. Fixture filtering реально работает.
4. Empty result не считается ошибкой.
5. Provider errors typed, без raw traceback.
6. FastAPI не пишет в DB.
7. Нет normalize/upsert/AI вне scope.

Ответ дай findings-first. Если блокеров нет, дай manual curl/httpie проверку.
```

## Daily result log

```md
# 2026-06-02

## Day status
DONE | PARTIAL | BLOCKED

## Goal
Реализовать POST /v1/eis223/search.

## Done
-

## Checks
-

## Evidence
- Search request:
- Search response:
- Tests:

## Decisions
-

## Blockers
-

## Next day input
Day 08 должен реализовать normalize endpoint и DTO, близкий к Prisma Tender/Document.
```
