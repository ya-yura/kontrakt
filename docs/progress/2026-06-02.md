# 2026-06-02

## Day status
BLOCKED

## Goal
- Record the Day 23 Sprint 05 live EIS/live document extraction status without overclaiming readiness.

## Done
- Confirmed that live EIS credentials are missing and only fixture/control documents may be used.
- Kept live EIS status as `BLOCKED: missing live EIS credentials`.
- Marked live document extraction as `BLOCKED_BY_MISSING_LIVE_EIS`.
- Confirmed fixture/control document extraction evidence remains valid only for pipeline mechanics and honest `TEXT_READY`/`OCR_REQUIRED`/`FAILED` states.
- Updated release readiness docs so live document extraction is not presented as ready.

## Checks
- No new live upstream EIS calls were made.
- No API keys or secret values were printed or committed.
- Previous targeted checks remain the current evidence:
  - `uv run pytest tests/test_settings.py tests/test_eis223_fixture_provider.py tests/test_eis223_search_endpoint.py tests/test_eis223_normalize_endpoint.py`: 20 passed.
  - `npm test -w apps/web`: 69 passed.

## Evidence
- Web health: previously verified `http://localhost:3103/api/healthz` returned `{"status":"ok","service":"web"}`.
- API health: previously verified `http://127.0.0.1:8001/healthz` returned EIS provider `fixture`.
- Screenshot/terminal output: not applicable; documentation/status update only.

## Decisions
- Do not claim live document extraction readiness while `EIS_PROVIDER_MODE=live`, `EIS_PROVIDER_BASE_URL`, and required `EIS_PROVIDER_API_KEY` are unavailable.
- Treat fixture/control document extraction as internal demo evidence only.
- Live document extraction status is `BLOCKED_BY_MISSING_LIVE_EIS` until real EIS provider credentials exist and real PDF/document retrieval is tested.

## Blockers
- `EIS_PROVIDER_MODE=live` is not configured.
- `EIS_PROVIDER_BASE_URL` is missing.
- Required `EIS_PROVIDER_API_KEY` is missing.
- Without live EIS provider access, real EIS tender documents cannot be fetched and live document extraction cannot be verified.

## Next day input
- Provide live EIS provider credentials in a secret-safe environment.
- Run a minimal live EIS smoke with 3-5 real tenders.
- Only after live tenders/documents are available, run live document download/file proxy/text extraction on real PDFs.
