# 2026-05-28

## Day status
DONE

## Goal
Добавить mock tenders и список закупок.

## Done
- Добавлен fixture/mock adapter `EIS223Adapter` для нормализованных 223-ФЗ закупок без live source integration.
- Добавлены 5 mock tenders для текущего dev user через Prisma seed.
- Для части закупок добавлены mock documents: 3, 2, 0, 2, 3 документа.
- Добавлены доменные поля `customerInn` и `decision` для списка закупок.
- Добавлена страница `/tenders` со списком закупок, сортировкой по deadline/price и empty state.
- В списке отдельно показаны `sourceStage` и `KanbanStage`.
- Добавлены ссылки на будущие карточки `/tenders/[id]`.
- Добавлена навигация на Tenders из Overview и Watchlists.

## Checks
- `npm run db:generate -w apps/web` - passed.
- `npm run db:migrate -w apps/web -- --name add_mock_tender_fields` - passed.
- `npm run db:seed -w apps/web` - passed.
- `npm test -w apps/web` - passed, 9 tests.
- `npm run typecheck -w apps/web` - passed.
- `npm run build -w apps/web` - passed.
- QA ownership check - passed: temporary tender owned by another user did not appear in dev user query or `/tenders` HTML.

## Evidence
- Seed result: dev user `dev.supplier@example.local` has 5 fixture tenders: `32414000123`, `32414000487`, `32414000991`, `32414001402`, `32414001977`.
- Tenders list URL: `http://127.0.0.1:3102/tenders`.
- Screenshot: pending manual capture from in-app browser; browser automation could not attach because the local browser runtime started in ESM mode and rejected its CommonJS bootstrap.

## Decisions
- Kept Day 04 in fixture/mock mode only; no real EIS API, FastAPI source integration, AI, or scoring engine was added.
- `sourceStage` remains a source lifecycle field; `KanbanStage` remains the internal workflow stage.
- The score column is a placeholder dash only, not a fake AI/scoring result.
- The adapter is isolated under `src/tenders/adapters` so Sprint 02 can replace fixture data with a live adapter.

## Blockers
- None for Day 04 acceptance criteria.
- Screenshot automation is blocked by local in-app browser runtime startup, so screenshot evidence should be captured manually from the open browser.

## Next day input
Day 05 должен начинаться с read-only tender card и недельного demo checkpoint.
