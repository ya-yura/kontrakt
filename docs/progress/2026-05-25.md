# 2026-05-25

## Day status
DONE

## Goal
Создать foundation монорепозитория.

## Done
- Создана структура `apps/web`, `apps/api`, `docs/progress`.
- Поднят минимальный Next.js App Router проект в `apps/web`.
- Поднят минимальный FastAPI проект в `apps/api`.
- Добавлен local PostgreSQL через `docker-compose.yml`.
- Добавлен полный безопасный `.env.example` без реальных секретов.
- Добавлены health endpoints: `GET /api/healthz` и `GET /healthz`.
- Добавлены README и базовые команды local dev.
- Добавлены минимальные scripts для install/dev/build/typecheck/test.
- Добавлен шаблон дневной фиксации в `docs/progress/daily-log-template.md`.

## Checks
- `npm run install:all` - passed.
- `npm run build` - passed.
- `npm run typecheck` - passed.
- `npm test` - passed.
- `docker compose ps postgres` - Postgres healthy.

## Evidence
- Web health: `GET http://127.0.0.1:3002/api/healthz` -> `200 {"status":"ok","service":"web"}`.
- API health: `GET http://127.0.0.1:8001/healthz` -> `200 {"status":"healthy","service":"api"}`.
- Screenshot/terminal output: `next build` compiled successfully; TypeScript and mypy found no issues; web health test passed; FastAPI pytest passed; Docker reported `operational-workspace-postgres` as `healthy` on `5439->5432`.

## Decisions
- PostgreSQL exposed on `localhost:5439` because local ports `5432-5438` were already occupied.
- FastAPI dev server binds to `127.0.0.1` by default for local-only development.
- Prisma is represented only by a placeholder directory for Day 02; no schema or migrations were added.
- No real external source, Auth, AI, OCR, Telegram, or email scope was added.

## Blockers
- None.

