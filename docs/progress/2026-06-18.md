# 2026-06-18

## Day status
DONE

## Goal
Реализовать AI panel и secure file proxy.

## Done
- Добавлен AI panel на tender card: статусы no analysis, pending, running, completed, failed, OCR_REQUIRED, text not ready.
- Panel показывает analyzed documents, OCR_REQUIRED/FAILED documents, source-backed `summaryMd`, required documents, critical risks, deadlines, evaluation criteria, unknowns, citations/source spans и confidence.
- Request AI analysis и Refresh AI analysis используют существующие actions и блокируются, когда document text не готов, нужен OCR, extraction failed или analysis уже pending/running.
- Добавлен `GET /api/files/[documentId]` с `auth()`, ownership через `document -> tender.ownerId`, safe content type, no-store/nosniff headers и generic safe errors.
- Document links на tender card идут через `/api/files/{documentId}`, без raw `sourceUrl` и без visible `storageKey`.
- Закрыт redirect allowlist bypass: file proxy обрабатывает redirects вручную, валидирует каждый next URL, ограничивает redirect chain и не возвращает upstream body при unsafe redirect.

## Checks
- `npm run typecheck -w apps/web` - passed.
- `npm run test -w apps/web` - passed, 69/69.

## Evidence
- AI panel completed: completed source-backed analysis test renders `summaryMd`, sections, confidence and citations/source spans.
- OCR_REQUIRED state: OCR_REQUIRED/FAILED test blocks actions and does not render fake summary.
- File proxy ownership: tests cover owned access, another user's document 404, missing document 404, arbitrary URL document id rejection, untrusted persisted URL rejection, allowlisted redirects, unsafe redirects, redirect limit and non-http Location blocking.

## Decisions
- AI output is presented as extractive and requiring human review; facts without valid `source_spans` are withheld from the authoritative panel.
- File proxy fetches only persisted document URLs after auth/ownership and allowlist checks; request input cannot supply an arbitrary URL.
- Redirects are manual with max 3 hops; every Location is resolved against the current URL and must remain http/https on an allowlisted host.
- File proxy errors stay generic and do not include source URLs, signed URLs, tokens or upstream bodies.

## Blockers
- None.

## Next day input
Day 20 должен закрыть release hardening, control AI sample и MVP demo.
