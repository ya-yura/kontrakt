# 2026-05-29

## Day status
DONE

## Goal
Собрать read-only tender card и weekly demo.

## Done
- Реализована страница `/tenders/[id]` для read-only карточки закупки.
- Добавлена ownership-проверка через owner-scoped Prisma query по текущему dev user.
- Собраны секции карточки: Header, Economics, Timeline, Requirements, Documents, Changes, AI.
- `sourceStage` и внутренний `KanbanStage` показаны отдельно.
- Quick actions `Rescore`, `Run AI`, `Move stage` добавлены как disabled/unavailable placeholders.
- Добавлены loading, error и not found states для tender card route.
- Расширены mock fixtures, seed и Prisma schema для нормализованных полей карточки.
- Подготовлены weekly demo notes в `docs/progress/week-01-demo.md`.

## Checks
- `npm run db:generate -w apps/web`
- `npm run typecheck -w apps/web`
- `npm run test -w apps/web`
- `npm run build -w apps/web`
- `npm run docker:up`
- `npx prisma migrate deploy`
- `npm run db:seed -w apps/web`
- HTTP fallback demo check на `http://localhost:3103/tenders` и первой `/tenders/[id]` карточке.

## Evidence
- Demo path: `/` -> `/watchlists` -> `/tenders` -> `/tenders/[id]`.
- Tender card URL: локально проверена первая карточка из seeded list, пример `http://localhost:3103/tenders/<seeded-id>`.
- Screenshot: не приложен; Browser plugin не запустился из-за локального `node_repl` ESM/`require` сбоя, поэтому проверка выполнена через HTTP.
- Weekly demo notes: `docs/progress/week-01-demo.md`.

## Decisions
- В Sprint 01 не реализовывались scoring engine, AI execution, source integration и Kanban movement.
- AI отображается только как честный empty state: "AI-анализ еще не запускался".
- Для read-only loop Next.js App Router остается system of record и единственным writer через Prisma.

## Blockers
- Product blocker отсутствует.
- Tooling limitation: Browser plugin недоступен в этой сессии из-за падения локального `node_repl` kernel; заменено HTTP-проверкой.

## Sprint 01 result
DONE

## Sprint 02 first task
Создать FastAPI provider abstraction и fixture/live mode.
