# 2026-06-05

## Day status
PARTIAL

## Goal
Закрыть sourceStage, source freshness и Sprint 02 demo.

## Done
- Реализован `deriveSourceStage` на стороне Next.js для `SUBMISSION_OPEN`, `COMMISSION_WORK`, `COMPLETED`, `CANCELED`, `EXPIRED`, `UNKNOWN`.
- `Tender.sourceStage`, `lastSeenAt`, `updatedFromSourceAt` и `providerMode` сохраняются при watchlist upsert.
- `kanbanStageId` остается независимым: upsert не двигает существующие закупки по kanban.
- Tender list/card показывают source freshness и явно не обещают real-time stream.
- Ручной `Refresh` watchlist вызывает auth/ownership-safe server action и `runWatchlists`.
- Добавлен regression test: повторный watchlist run не создает duplicate tenders/documents и не перетирает пользовательский kanban stage.
- Подготовлены Sprint 02 weekly demo notes.

## Checks
- `npm test` в `apps/web`: 19 passed.
- `npm run typecheck` в `apps/web`: passed.
- `npm run build` в `apps/web`: passed.

## Evidence
- deriveSourceStage tests: notice/documentation open, protocol, result/final protocol, canceled, expired, unknown.
- Source freshness UI: `/tenders` и `/tenders/{id}` показывают `lastSeenAt`, `updatedFromSourceAt`, `providerMode` и non-real-time notice.
- Control sample: fixture-based 3 закупки проверены; live 20 закупок BLOCKED без live source config/API key.
- Weekly demo notes: `docs/progress/week-02-demo.md`.

## Decisions
- Source stage является source-derived подсказкой, а не автоматическим kanban movement.
- `updatedFromSourceAt` хранит только upstream timestamp, если он есть; `lastSeenAt` фиксирует refresh в workspace.
- Fixture demo можно показывать как deterministic fixture, но нельзя называть live validation.

## Blockers
- Live EIS/FastAPI env не настроен: нет подтвержденной 20-закупочной live control sample.
- Нужно применить Prisma migrations в dev database перед следующей live проверкой.

## Sprint 02 result
PARTIAL

## Sprint 03 first task
Начать с Kanban board и ownership-safe `moveTenderToStage`.
