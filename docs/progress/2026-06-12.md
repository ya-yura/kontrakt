# 2026-06-12

## Day status
DONE

## Goal
Добавить alert adapters и закрыть Sprint 03 demo.

## Done
- Добавлены Email и Telegram alert delivery adapters с `mock|live` режимом.
- Missing Email/Telegram secrets переводят delivery в safe stub, без raw exception.
- Live provider/network exceptions не bubble-ятся наружу и записываются как typed failed delivery.
- Alert text ограничен безопасными metadata: номер, заказчик, событие, дедлайн, deep link.
- `alerts/run` учитывает `notifyEmail`, `notifyTelegram` и nested `alertPreferences`.
- Alert history показывает channel, type, mode/status, `sentAt`, ack и safe error.
- Weekly Sprint 03 demo notes созданы.

## Checks
- `npm test` in `apps/web`: 47 passed.
- `npm run typecheck` in `apps/web`: passed.

## Evidence
- Alert delivery mode: `ALERT_DELIVERY_MODE=mock|live`; mock valid for demo, live ready for env secrets.
- Alert text: only number, customer, event, deadline, deep link; no attachments, raw payload, `sourcePayload`, or AI raw response.
- Demo path: `/tenders` -> `/board` move -> tender checklist -> rescore -> `POST /api/cron/alerts/run` -> alert history.
- Weekly demo notes: `docs/progress/week-03-demo.md`, with mock/live boundary and live sending marked unverified.

## Decisions
- Sprint 03 can be marked DONE in mock mode; live Email/Telegram sending is not required for DONE.
- Provider runtime failures use safe generic messages and typed failed delivery results.
- Alert history displays derived mode/status for operator clarity before adding a persisted status field.

## Blockers
- None for Sprint 03.

## Sprint 03 result
DONE

## Sprint 04 first task
Начать с document text extraction: TEXT_READY/OCR_REQUIRED/FAILED без fake AI summary.
